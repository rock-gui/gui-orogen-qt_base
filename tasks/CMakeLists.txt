# Generated from orogen/lib/orogen/templates/tasks/CMakeLists.txt

include(qt_baseTaskLib)
ADD_LIBRARY(${QT_BASE_TASKLIB_NAME} SHARED
    ${QT_BASE_TASKLIB_SOURCES})
add_dependencies(${QT_BASE_TASKLIB_NAME}
    regen-typekit)

find_package(Qt4 REQUIRED)
include(${QT_USE_FILE})
include_directories(${QT_INCLUDE_DIR})
link_directories(${QT_LIBRARY_DIR})
set(CMAKE_AUTOMOC true)

TARGET_LINK_LIBRARIES(${QT_BASE_TASKLIB_NAME}
    ${OrocosRTT_LIBRARIES}
    ${QT_LIBRARIES}
    ${QT_BASE_TASKLIB_DEPENDENT_LIBRARIES})
SET_TARGET_PROPERTIES(${QT_BASE_TASKLIB_NAME}
    PROPERTIES LINK_INTERFACE_LIBRARIES "${QT_BASE_TASKLIB_INTERFACE_LIBRARIES}")
SET_TARGET_PROPERTIES(${QT_BASE_TASKLIB_NAME}
    PROPERTIES INTERFACE_LINK_LIBRARIES "${QT_BASE_TASKLIB_INTERFACE_LIBRARIES}")

INSTALL(TARGETS ${QT_BASE_TASKLIB_NAME}
    RUNTIME DESTINATION bin
    LIBRARY DESTINATION lib/orocos)

INSTALL(FILES ${QT_BASE_TASKLIB_HEADERS}
    DESTINATION include/orocos/qt_base)

